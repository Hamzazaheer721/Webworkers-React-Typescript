/* eslint-disable no-unused-vars */
type IGearUpSettingsProps = {
  itemHeight: number,
  amount: number,
  tolerance: number,
  minIndex: number,
  maxIndex: number,
  startIndex: number
}

const gearUpSettings = (value : IGearUpSettingsProps) => {

}

export default gearUpSettings;
